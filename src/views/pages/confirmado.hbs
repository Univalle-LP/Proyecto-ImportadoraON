{{!< ../layouts/main}}

<div class="max-w-lg mx-auto mt-12 bg-white rounded shadow p-8 text-center">
  <h2 class="text-2xl font-bold text-green-600 mb-4">¡Compra confirmada!</h2>
  {{#if mensaje}}
    <p class="text-gray-700 mb-4">{{mensaje}}</p>
  {{else}}
    <p class="text-gray-700 mb-4">Tu pedido ha sido registrado exitosamente.</p>
  {{/if}}
  <a href="/productos" class="inline-block mt-6 bg-blue-600 hover:bg-blue-700 text-white px-6 py-2 rounded-full font-semibold transition">Volver a productos</a>
</div>